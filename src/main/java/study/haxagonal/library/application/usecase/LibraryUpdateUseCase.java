package study.haxagonal.library.application.usecase;

import study.haxagonal.library.application.domain.Library;

public interface LibraryUpdateUseCase {
    Library updateLibrary(Library library);
}
